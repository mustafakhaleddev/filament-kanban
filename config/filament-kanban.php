<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Column Width
    |--------------------------------------------------------------------------
    */
    'column_width' => '280px',

    /*
    |--------------------------------------------------------------------------
    | Default Records Per Column
    |--------------------------------------------------------------------------
    | Set to null for unlimited records per column.
    */
    'records_per_column' => null,

    /*
    |--------------------------------------------------------------------------
    | Default Card View
    |--------------------------------------------------------------------------
    */
    'card_view' => 'filament-kanban::components.card',

    /*
    |--------------------------------------------------------------------------
    | Default Order Column
    |--------------------------------------------------------------------------
    | Set to a column name (e.g. 'sort_order') to enable within-column
    | reordering by default. Set to null to disable.
    */
    'default_order_column' => null,

    /*
    |--------------------------------------------------------------------------
    | Animation Duration (ms)
    |--------------------------------------------------------------------------
    */
    'animation_duration' => 150,

];
